# IntraSphere - Configuration Apache Minimale
# Version compatible avec la plupart des hébergeurs mutualisés

# Protection des fichiers sensibles (syntaxe compatible Apache 2.2/2.4)
<FilesMatch "^\.env$">
    Require all denied
</FilesMatch>

<FilesMatch "\.sql$">
    Require all denied
</FilesMatch>

# Permettre l'accès aux fichiers d'installation
<FilesMatch "^(install|reset_installation)\.php$">
    Require all granted
</FilesMatch>

# Protection des dossiers système
<DirectoryMatch "(config|logs|tmp|src)">
    Require all denied
</DirectoryMatch>